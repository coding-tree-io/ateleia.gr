---
import { PaintedEdgeDivider } from '@/components/decorative/ArtShapes';
import { About } from '@/components/sections/About';
import { Contact } from '@/components/sections/Contact';
import { Footer } from '@/components/sections/Footer';
import { HeaderCtaVisibilityObserver } from '@/components/sections/HeaderCtaVisibilityObserver';
import { Hero } from '@/components/sections/Hero';
import SiteHeader from '@/components/sections/SiteHeader.astro';
import { Services } from '@/components/sections/Services';
import { WhatIs } from '@/components/sections/WhatIs';
import { WhoIsItFor } from '@/components/sections/WhoIsItFor';
import PublicDocumentLayout from '@/layouts/PublicDocumentLayout.astro';

const topDividerClassName = 'painted-divider -mt-1 h-3 w-full opacity-28 md:h-10 md:opacity-60';
const sectionDividerClassName = 'painted-divider -mt-1 h-2 w-full opacity-22 md:h-8 md:opacity-55';
const sectionDividerInvertedClassName =
  'painted-divider -mt-1 h-2 w-full rotate-180 opacity-22 md:h-8 md:opacity-55';
---

<PublicDocumentLayout
  pageTitle="Ατέλεια | Θεραπεία μέσω τέχνης"
  pageDescription="Ατέλεια — Θεραπεία μέσω τέχνης. Ένας ασφαλής και δημιουργικός χώρος για προσωπική εξέλιξη μέσα από τη δημιουργική έκφραση."
  htmlClassName="hero-in-view"
  includeCodingTreeAttributionScript
>
  <div class="min-h-screen overflow-x-clip bg-background font-sans text-foreground">
    <SiteHeader />
    <HeaderCtaVisibilityObserver client:load />

    <main class="relative overflow-x-clip overflow-y-visible">
      <Hero />
      <PaintedEdgeDivider className={topDividerClassName} />

      <div id="what-is">
        <WhatIs />
      </div>
      <PaintedEdgeDivider className={sectionDividerInvertedClassName} />

      <div id="who-is-it-for">
        <WhoIsItFor />
      </div>
      <PaintedEdgeDivider className={sectionDividerClassName} />

      <About />
      <PaintedEdgeDivider className={sectionDividerInvertedClassName} />

      <Services />
      <PaintedEdgeDivider className={sectionDividerClassName} />

      <Contact />
    </main>

    <Footer />
  </div>
</PublicDocumentLayout>
